services:
  # PostgreSQL
  postgres:
    image: pgvector/pgvector:pg16
    container_name: recom_postgres
    environment:
      POSTGRES_DB: eshop
      POSTGRES_USER: app
      POSTGRES_PASSWORD: secret
      POSTGRES_HOST_AUTH_METHOD: md5
    ports:
      - "5434:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - eshop-network

  # Redis
  redis:
    image: redis:7-alpine
    container_name: eshop-redis
    ports:
      - "6380:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - eshop-network

  # SCHEDULER SERVICE
  scheduler:
    build:
      context: ..
      dockerfile: Docker/Dockerfile
    container_name: eshop-scheduler
    command: python scheduler.py --daemon
    environment:
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_NAME=eshop
      - DB_USER=app
      - DB_PASSWORD=secret
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - SCHEDULER_CONFIG=/app/scheduler_config.json
    volumes:
      - ../etl/data:/app/data
      - ../etl/logs:/app/logs
      - ../etl/scheduler.py:/app/scheduler.py
      - ../etl/config.py:/app/config.py
      - ../etl/Content_Base_Model/scheduler_api.py:/app/scheduler_api.py
      - ./scheduler_config.json:/app/scheduler_config.json
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - eshop-network
    restart: unless-stopped
    profiles:
      - scheduler

  # ETL
  etl:
    build:
      context: ..
      dockerfile: Docker/Dockerfile
    container_name: eshop-etl
    entrypoint: ["/app/entrypoint.sh"]
    command: ["etl", "--lookback-days", "90"]
    environment:
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_NAME=eshop
      - DB_USER=app
      - DB_PASSWORD=secret
    volumes:
      - ../etl/data:/app/data
      - ../etl/logs:/app/logs
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - eshop-network
    profiles:
      - etl

  # CLIP Embeddings
  clip-embeddings:
    build:
      context: ..
      dockerfile: Docker/Dockerfile
    container_name: eshop-clip
    entrypoint: ["/app/entrypoint.sh"]
    command: ["clip"]
    environment:
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_NAME=eshop
      - DB_USER=app
      - DB_PASSWORD=secret
    volumes:
      - ../etl/data:/app/data
      - ../etl/logs:/app/logs
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - eshop-network
    profiles:
      - clip

  # Workflow
  workflow:
    build:
      context: ..
      dockerfile: Docker/Dockerfile
    container_name: eshop-workflow
    entrypoint: ["/app/entrypoint.sh"]
    command: ["workflow"]
    environment:
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_NAME=eshop
      - DB_USER=app
      - DB_PASSWORD=secret
    volumes:
      - ../etl/data:/app/data
      - ../etl/logs:/app/logs
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - eshop-network
    profiles:
      - workflow

  # Build FAISS
  build-faiss:
    build:
      context: ..
      dockerfile: Docker/Dockerfile
    container_name: eshop-build-faiss
    entrypoint: ["/app/entrypoint.sh"]
    command: ["build-faiss"]
    volumes:
      - ../etl/data:/app/data
      - ../etl/logs:/app/logs
    networks:
      - eshop-network
    profiles:
      - build-index

  # FAISS API
  api:
    build:
      context: ..
      dockerfile: Docker/Dockerfile
    container_name: eshop-api-cpu
    command: uvicorn Content_Base_Model.faiss_api:eshop --host 0.0.0.0 --port 8000 --reload
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - DB_HOST=postgres
      - DB_PORT=5432
    ports:
      - "8000:8000"
    volumes:
      - ../etl/data:/app/data
      - ../etl/logs:/app/logs
      - ../etl/Content_Base_Model:/app/Content_Base_Model
      - ../etl/config.py:/app/config.py
    depends_on:
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy
    networks:
      - eshop-network
    restart: unless-stopped

  # Gradio Tester
  gradio-tester:
    build:
      context: ..
      dockerfile: Docker/Dockerfile
    container_name: eshop-gradio
    command: python testAPI.py
    environment:
      - API_URL=http://api:8000
    ports:
      - "7860:7860"
    volumes:
      - ../etl/data:/app/data
      - ../etl/testAPI.py:/app/testAPI.py
    depends_on:
      - api
    networks:
      - eshop-network
    profiles:
      - testing

volumes:
  postgres_data:
    driver: local
  redis_data:
    driver: local

networks:
  eshop-network:
    driver: bridge
