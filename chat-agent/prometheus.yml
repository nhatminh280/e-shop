global:
  scrape_interval: 15s
  scrape_timeout: 10s

scrape_configs:
  - job_name: chat-agent
    metrics_path: /agent/metrics
    static_configs:
      - targets:
          - chat-agent:8010
        labels:
          service: chat-agent
          env: production
